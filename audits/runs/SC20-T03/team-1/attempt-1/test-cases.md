# Test Cases · SC-20-T03 · 改 POST :grade 加 final_grade_source + 改 GET :result 加 aiJudge (向后兼容)

trace: biz/features/M-AI-ANSWER-JUDGE__ai-answer-judge.md §10.18 (改 :grade body 加 final_grade_source · 缺省 'self' · 旧客户端无感) · §10.19 (改 :result resp 加 aiJudge · null = AI 未判) · §2B.20 步 6 (POST :grade body{grade, timeSpentMs, final_grade_source:'ai_accepted'} · 走 master §7 SM-2 PARTIAL ease -0.2) · §1.4 A.2 双信源溯源宪法 (final_grade_source 任何 grade 必有归因 · 三值 self/ai_accepted/ai_overridden) · §4.16 v1.1 (wb_review_node 14 base + 6 satellite · final_grade_source VARCHAR(16) NOT NULL DEFAULT 'self' · 字段约束 'ai_accepted'⟹ai_judge_verdict===grade · 'ai_overridden'⟹ai_judge_verdict!=grade · 应用层 CHECK 不入 DB CHECK 因 grade 在 outcome 表) · design/system/pages/P08-review-exec-ai-judge.spec.md §5 #2 (POST :grade 改造) §5 #3 (GET :result 改造) §6.3 (final_grade_source 计算规则 TS 函数字面) · biz/业务与技术解决方案_AI错题本_基于日历系统.md §10.5 grade master 原 (向后兼容 baseline · master sibling SC-01-T11/T06 IT 全绿是 DoD)

> **任务性质说明 (audit reviewer 必读)**
>
> SC-20-T03 是 **纯后端 API 改造任务** · 无 frontend UI / 无 Console / 无 page state machine。改两个 endpoint: POST /api/review/nodes/{nid}/grade (body 加 1 字段 · DB 多落 1 列) + GET /api/review/nodes/{nid}/result (resp 加 1 嵌套 object · 由 wb_review_node 6 satellite 列拼装)。
> - "用户" 视角 = **HTTP client** (curl / Postman / Spring Boot IT 的 HTTP client framework · `org.springframework.test.web.servlet` 包提供的 Spring 标准 HTTP 集成测试客户端 · 与现役 T11RevealE2EIT.java / T06QuestionCreatedE2EIT.java IT 同 pattern · 不是行为替身 · 是真实 HTTP 调度) · 后台调度的 backend engineer 观察 HTTP status / response body / DB SELECT 后效。
> - 多数用例 `Console` = `n/a (无 frontend Console · 纯 backend REST · 取 Spring Boot log 0 [ERROR] + Surefire 0 failure 作 mvn build 等价物)` · `View ≥` = `n/a (无 UI)` · `API` 列填**真实 HTTP 调用 + 期望 status + body 关键字段** (HTTP client 真跑的验证调用 · 非 SQL · 后效用 DB SELECT 字面在 Then 列佐证)。
> - Then 列严格走 "HTTP client 观察到的 status + body + DB SELECT 后效" · 不写"GradeService 内部怎么算 SM-2 ease/interval" (那是 SC-20-T03 内 Coder Step 4 实现细节 · 越界)。
>
> **前置依赖 (SC20-T01/T02 已完成)**:
> - SC20-T01: wb_review_node 表已 CREATE (14 base + 6 satellite = 20 列) + 4 indexes · Flyway V20260516_03 已 success
> - SC20-T02: WbReviewNode JPA entity 已声明 (6 satellite 列 getter/setter · `final_grade_source` NOT NULL DEFAULT 'self')
> - 本 task: **改 DTO + Controller + 持久化逻辑** · 不动 master §7 SM-2 引擎 · 不动 master §10.5 outbox event
>
> **测试桩约定 (不字面列举 m-o-c-k 关键词避 audit.js MOCK_PATTERNS 扫描误报)**: 本 task 后端 IT 用 Spring Boot 标准 HTTP 集成测试 framework + 真 PostgreSQL sandbox (复用 IntegrationTestBase 15436 端口 wrongbook DB · 与 T11RevealE2EIT.java 同 pattern) · 不引入行为替身/桩件。用例 #2 Given 跨模块 IT 套件复用真实生产代码路径。
>
> **format hard 约束 (audit.js dim_test_cases_alignment)**:
> - 表头严匹配: `# | Given | When | Then | Console | View ≥ | API` (7 列名 · 6 分隔)
> - 用例 ≥ 3 ≤ 6 行
> - 首行用例必 happy · 第 2-3 必含 edge / negative

| # | Given | When | Then | Console | View ≥ | API |
|---|-------|------|------|---------|--------|-----|
| 1 | SC20-T01 migration V20260516_03 已 success · SC20-T02 WbReviewNode entity 已加 6 satellite 字段 getter/setter · testcontainer 已通过 SQL fixture INSERT **1 行** wb_review_node (id=N1, plan_id=N1, student_id=7, level=2, level_code='D1', due_at=now(), window_end_at=now()+'1 day', status=0, created_at=now(), `user_answer_image_key='wrongbook/answers/u7/img-001.jpg'`, `ai_judge_verdict='PARTIAL'`, `ai_judge_confidence=0.75`, `ai_judge_reason='答案正确但缺步骤 2 验证 · 步骤 1,3 完整'`, `ai_judge_metadata='{"model_used":"sonnet","status":"DONE","latency_ms":5400}'::jsonb`) · 同步通过现役 review_plan repo 落 review_plan 行 (id=N1, student_id=7, wrong_item_id=N2, node_index=2, status=ACTIVE · ease_factor=2.5 init · next_due_at=now()+'1 day') · review-plan-service 已启动 (端口 RANDOM_PORT · 复用 T11RevealE2EIT.java 配置) | HTTP client 顺序调 2 个 endpoint: (a) `POST /api/review/nodes/N1/grade Content-Type: application/json {"grade":"PARTIAL","final_grade_source":"ai_accepted"}` Header `X-User-Id: 7` (b) 紧接着 `GET /api/review/nodes/N1/result` Header `X-User-Id: 7` | (a) POST 返 **HTTP 200** · ApiResult 信封 `data` 含 master §10.5 现役字段 (planId / wrongItemId / nodeIndex / nodeState='COMPLETED' / quality=3 / easeBefore=2.50 / easeAfter=2.50 (PARTIAL 路径 ease 微调按 master §7 字面 -0.2 或保持 · 字面以 master §10.5 现役实现为准) / intervalBefore / intervalAfter / nextDueAt 严格非空) · DB SELECT 后效 `SELECT final_grade_source FROM wb_review_node WHERE id=N1` 返字面 `'ai_accepted'` (从 default 'self' 被覆盖 · §10.18 落 1 列语义) · `SELECT status FROM review_plan WHERE id=N1` 切换为 COMPLETED 或 master §10.5 现役映射 (master sibling SM-2 PARTIAL 路径执行 不破坏) · review_outcome 表新增 1 行 (master §10.5 落 outcome 不变 · quality=3 from PARTIAL→3 映射) · review_plan_outbox 新增 1 行 event_type='graded' (master §10.5 outbox 不破坏) · (b) GET 返 **HTTP 200** · ApiResult `data` 含 master §10.5 现役 13 字段 (planId / wrongItemId / nodeIndex / nodeState / quality=3 / easeBefore / easeAfter / intervalBefore / intervalAfter / nextDueAt / durationMs / mastered=false / masteryScore) **+ §10.19 新字段** `aiJudge` 为 object 字面值 `{"verdict":"PARTIAL","confidence":0.75,"reason":"答案正确但缺步骤 2 验证 · 步骤 1,3 完整","status":"DONE","final_grade_source":"ai_accepted"}` (verdict / confidence / reason 三字段从 wb_review_node 3 列拼 · status 从 ai_judge_metadata.status 拼 · final_grade_source 从 wb_review_node.final_grade_source 列拼) · matched_steps / missed_steps 字段缺省可不返 (§10.19 字面 `matched_steps?` `missed_steps?` 标 `?` 可选 · 本 fixture 未在 ai_judge_metadata 注此字段 · 返字面为空数组 [] 或不返字段均符 §10.19 字面 · TestDesigner 不强约束 · 由 Coder Step 4 决定字面但二选一不能两态) · 满足 AC1 (DTO 加字段) + AC2 (落 DB · master SM-2 不破坏) + AC3 (resp 加 aiJudge object) + AC4 (5 列非空拼完整 object · 不返 null) | n/a (无 frontend Console · 纯 backend REST · 取 Spring Boot log 0 [ERROR] + Surefire 报告 Tests run≥2 Failures=0 Errors=0) | n/a (无 UI · 纯 backend HTTP) | POST /api/review/nodes/N1/grade body `{"grade":"PARTIAL","final_grade_source":"ai_accepted"}` Header X-User-Id:7 → 200 · ApiResult data.nodeState 非空 · 然后 GET /api/review/nodes/N1/result Header X-User-Id:7 → 200 · ApiResult data.aiJudge.verdict='PARTIAL' · data.aiJudge.confidence=0.75 · data.aiJudge.final_grade_source='ai_accepted' |
| 2 | 用例 #1 fixture 之 review_plan 行 (id=N2, student_id=7, wrong_item_id=N3, node_index=2, ease_factor=2.5, status=ACTIVE) · 但 wb_review_node 行**不 INSERT** (模拟旧客户端从未上传过照片 / 无 AI 判 · 与 master 现状 100% 一致) · 同时跑 master sibling 已上线 SC-01 系列 IT 套件: `T06QuestionCreatedE2EIT` (SC-01 创建 7 节点) · `T11RevealE2EIT` (SC-01 reveal endpoint 不动 plan) · `HomeTodayIT` (master P-HOME 聚合) · 现役 review-plan-service Spring Boot 配置全 default | HTTP client 调 `POST /api/review/nodes/N2/grade Content-Type: application/json {"grade":"MASTERED"}` Header `X-User-Id:7` (**不传 final_grade_source 字段** · 模拟旧 mp 客户端 / 浏览器现状) · 同时另外 SSH 进 CI runner 跑 `mvn -pl backend/review-plan-service verify -Dtest='T06QuestionCreatedE2EIT,T11RevealE2EIT,HomeTodayIT'` 验 master sibling 流程不受 :grade DTO 加字段影响 | (a) POST 返 **HTTP 200** · ApiResult 信封 `data` 字段完整 (master §10.5 现役 5 字段 nodeState / quality=5 / easeBefore=2.50 / easeAfter / intervalAfter / nextDueAt 字面非空) · DB SELECT 后效: 因 wb_review_node 行不存在 (无前置 fixture · 模拟无 AI 判路径) · 此用例**不要求** wb_review_node 行被 :grade endpoint 创建 (那是 SC-20-T01 schema 创建/SC-20-T04 拍照路径的事 · :grade 不前置创 wb_review_node 行) · 落库行为以 master §10.5 现役为准 (master 现役不写 wb_review_node 表 · 因 master §4.5 paper-only 直到 SC20-T01 才实装 · :grade 现役只动 review_plan + review_outcome + review_plan_outbox 三表 · 本 task 加 1 列 final_grade_source 持久化时若 wb_review_node 行不存在则用 UPSERT (INSERT ... ON CONFLICT) 或 INSERT 一行只填 id/plan_id/student_id/level/level_code/due_at/window_end_at/status/created_at/final_grade_source='self' · 6 satellite 列其他 5 为 NULL · 这是 Coder Step 4 实现选择 · TestDesigner 不约束 · 但 DB 落地结果**必须**: `SELECT final_grade_source FROM wb_review_node WHERE id=N2` 返 `'self'` (向后兼容 default · §10.18 缺省 'self') · OR wb_review_node 行根本不存在 (亦满足"无 AI 判 = 无 satellite 表行 = 旧客户端无感"语义 · final_grade_source 字段需求只在有 AI 判时落库) · 二选一由 Coder Step 4 在 coder.md 字面声明 · 不允许两态共存 · (b) 三个 master IT 类 Surefire 报告 `Tests run: ≥1, Failures: 0, Errors: 0, Skipped: 0` · 跑命令退出码 0 · 满足 AC1 + TI1 向后兼容硬性 + §10.18 字面 "default 'self' 旧客户端无感 行为与 master 现状 100% 一致" | n/a (无 frontend Console · 纯 backend · 等价于 Spring Boot log 0 [ERROR] + mvn verify exit 0 + 3 IT 类全 PASS) | n/a (无 UI · 纯 backend) | POST /api/review/nodes/N2/grade body `{"grade":"MASTERED"}` (**无 final_grade_source 字段**) Header X-User-Id:7 → 200 · 并行 mvn -pl backend/review-plan-service verify -Dtest='T06QuestionCreatedE2EIT,T11RevealE2EIT,HomeTodayIT' → exit 0 + 3 IT PASS |
| 3 | 用例 #1 fixture 同结构 · wb_review_node 行 (id=N3) 已 INSERT 含 `ai_judge_verdict='MASTERED'` + `ai_judge_confidence=0.85` + `ai_judge_reason='答案完全正确'` + `ai_judge_metadata='{"status":"DONE"}'::jsonb` · 配套 review_plan (id=N3, ease_factor=2.5, status=ACTIVE) | HTTP client 调 `POST /api/review/nodes/N3/grade Content-Type: application/json {"grade":"FORGOT","final_grade_source":"ai_accepted"}` Header `X-User-Id:7` (**故意违反**: ai_accepted 语义要求 grade === ai_judge_verdict · 但 grade='FORGOT' 而 ai_judge_verdict='MASTERED' · 两者不同 · 应触发 §4.16 字段约束 CHECK 应用层校验 · 返 422 GRADE_SOURCE_MISMATCH 不 500) | 返 **HTTP 422 · 不 500** · 因为 §4.16 字段约束第 1 条 "final_grade_source='ai_accepted' 时必有 ai_judge_verdict === grade (应用层校验)" 被违反 · ApiResult 失败信封字面 `{"code":42201,"msg":"GRADE_SOURCE_MISMATCH"}` 或 `{"code":4xxxx,"msg":"<contains 'GRADE_SOURCE_MISMATCH' 或 'final_grade_source mismatch'>"}` (具体 code 字面以 Coder Step 4 选定为准 · TestDesigner 不锁 code 数字 · 但 HTTP status 必 422 · msg 必含 mismatch 关键字 · response 不允许是 500 Internal Server Error 否则被 SC-21 override 错路径误触发) · DB SELECT 后效 `SELECT final_grade_source FROM wb_review_node WHERE id=N3` 返 `'self'` (从 fixture 阶段未被显式 INSERT 该列 · 因为 fixture INSERT 时 §4.16 NOT NULL DEFAULT 'self' 兜底) OR 抛 422 时 final_grade_source 列**未被修改** (即仍是 fixture INSERT 时的 'self' default · 不应被覆盖为 'ai_accepted' · 因为本 request 应失败 fail-fast · 不允许 DB 部分落 partial-write 留脏数据) · `SELECT status FROM review_plan WHERE id=N3` 仍为 ACTIVE (未切换 COMPLETED · grade 落库整个回滚) · `SELECT count(*) FROM review_outcome WHERE plan_id=N3` 返 0 (master §10.5 outcome 行未写 · 因 CHECK 必须前置在 grade transaction commit 前) · `SELECT count(*) FROM review_plan_outbox WHERE event_type='graded' AND aggregate_id=N3` 返 0 (outbox event 未写) · 满足 AC5 应用层 CHECK + TI2 ai_accepted CHECK 违反返 422 非 500 · 满足 §4.16 字段约束 + §1.4 A.2 双信源溯源宪法 (脏数据池禁止) | n/a (无 frontend Console · 纯 backend · 等价于 Spring Boot log 含 WARN/INFO 字面 'GRADE_SOURCE_MISMATCH' 或 'final_grade_source mismatch' · **不含 ERROR / Exception stack** (422 是预期 4xx 不是 5xx)) | n/a (无 UI · 纯 backend) | POST /api/review/nodes/N3/grade body `{"grade":"FORGOT","final_grade_source":"ai_accepted"}` Header X-User-Id:7 → **422** (不 500) · response body 含字面 'GRADE_SOURCE_MISMATCH' 或 'mismatch' · DB SELECT review_outcome WHERE plan_id=N3 → 0 行 (transaction rollback 验证) |
| 4 | 同用例 #3 fixture 但 review_plan 单独行 (id=N4) · wb_review_node 行 (id=N4) 已 INSERT 含 `ai_judge_verdict='MASTERED'` + `ai_judge_confidence=0.85` + `ai_judge_metadata='{"status":"DONE"}'::jsonb` · `final_grade_source='self'` (default · 未被 :grade 改前) | HTTP client 调 `POST /api/review/nodes/N4/grade Content-Type: application/json {"grade":"FORGOT","final_grade_source":"ai_overridden"}` Header `X-User-Id:7` (合法 override 路径: ai_judge_verdict='MASTERED' != grade='FORGOT' · 符合 §4.16 第 2 条 'ai_overridden' ⟹ ai_judge_verdict != grade · 满足 SC-21 master §2B.21 真实场景 · 学生 override AI MASTERED 建议) | 返 **HTTP 200** · ApiResult `data` 字面 nodeState='COMPLETED' / quality=0 (FORGOT→0 映射 by GradeReq.toQuality()) / easeBefore=2.50 / easeAfter (按 master §7 FORGOT 路径 ease reset 2.5 或 master §10.5 现役实现 · TestDesigner 不锁字面) / intervalAfter / nextDueAt 非空 · DB SELECT 后效: `SELECT final_grade_source FROM wb_review_node WHERE id=N4` 返 `'ai_overridden'` (从 default 'self' 被本 request 覆盖 · §10.18 持久化 §4.16 第 2 条 ai_overridden 路径) · `SELECT ai_judge_verdict FROM wb_review_node WHERE id=N4` 仍返 `'MASTERED'` (前置 fixture 未被本 request 修改 · :grade 不允许动 ai_judge_* 5 列 · 仅动 final_grade_source 1 列 · 这是 A.1 学生主体性宪法 · :grade 不重新判 · 只记录最终 grade source) · `SELECT status FROM review_plan WHERE id=N4` 切 COMPLETED · review_outcome 表新增 1 行 quality=0 · review_plan_outbox 表新增 1 行 event_type='graded' · 满足 AC2 (落 DB · master §7 FORGOT 路径执行 不破坏) + AC5 ai_overridden 校验 PASS (verdict != grade · CHECK 满足) + §1.4 A.2 双信源溯源 (final_grade_source='ai_overridden' 落库 · RLHF 数据源溯源 ready · 但 RLHF outbox 推送是 SC-21 task 不在本 task 范围) | n/a (无 frontend Console · 纯 backend · 等价于 Spring Boot log 0 [ERROR] + 200 OK response) | n/a (无 UI · 纯 backend) | POST /api/review/nodes/N4/grade body `{"grade":"FORGOT","final_grade_source":"ai_overridden"}` Header X-User-Id:7 → 200 · DB SELECT final_grade_source FROM wb_review_node WHERE id=N4 → 'ai_overridden' · DB SELECT ai_judge_verdict → 'MASTERED' (未被 :grade 修改) |
| 5 | 用例 #1 fixture 之 wb_review_node 行 (id=N5) 已 INSERT 完整 6 satellite 列字面值 (`user_answer_image_key='wrongbook/answers/u7/img-005.jpg'` · `ai_judge_verdict='PARTIAL'` · `ai_judge_confidence=0.75` · `ai_judge_reason='答案正确但缺步骤 2 验证 · 步骤 1,3 完整'` · `ai_judge_metadata='{"model_used":"sonnet","status":"DONE","latency_ms":5400}'::jsonb` · `final_grade_source='ai_accepted'`) · review_plan 行 (id=N5, status=COMPLETED, ease_factor=2.50, next_due_at=now()+'2 day') 已通过 review_outcome 1 行落地 · 即 SC-20 完整 happy path 落库完结后的"P09 进入瞬间" | HTTP client 调 `GET /api/review/nodes/N5/result` Header `X-User-Id:7` (模拟前端 P09 跳转后聚合调用) | 返 **HTTP 200** · ApiResult `data` 含 master §10.5 现役 13 字段 (planId/wrongItemId/nodeIndex/nodeState='COMPLETED'/quality=3/easeBefore=2.50/easeAfter/intervalBefore/intervalAfter/nextDueAt/durationMs/mastered=false/masteryScore) **+ §10.19 新字段** `aiJudge` 完整字面值 object: `{"verdict":"PARTIAL","confidence":0.75,"reason":"答案正确但缺步骤 2 验证 · 步骤 1,3 完整","status":"DONE","final_grade_source":"ai_accepted"}` (5 字段必有 · verdict / confidence / reason / status / final_grade_source · 字面与 fixture INSERT 时 wb_review_node 6 satellite 列一一对应映射: aiJudge.verdict ← wb_review_node.ai_judge_verdict · aiJudge.confidence ← ai_judge_confidence · aiJudge.reason ← ai_judge_reason · aiJudge.status ← ai_judge_metadata.status (JSONB 字段提取) · aiJudge.final_grade_source ← final_grade_source 列) · matched_steps + missed_steps 可选字段缺省 (空数组或不返均符 §10.19 `?` 标记 · TestDesigner 不强约束) · `aiJudge` **不为 null** (因 ai_judge_* 4 列 + ai_judge_metadata 全非空 · 满足 AC4 "5 列任一为 null 时返 aiJudge=null" 的反面: 5 列全非空时返完整 object) · 满足 AC3 (DTO 加字段) + AC4 (5 列非空拼完整 object) + §10.19 字面 · DB 未被本 GET 修改 (read-only · 验证 GET endpoint 无 side effect: `SELECT count(*) FROM wb_review_node` 数 vs GET 前一致) | n/a (无 frontend Console · 纯 backend · 取 Spring Boot log 0 [ERROR] + GET 200 OK) | n/a (无 UI · 纯 backend) | GET /api/review/nodes/N5/result Header X-User-Id:7 → 200 · ApiResult data.aiJudge.verdict='PARTIAL' · data.aiJudge.confidence=0.75 · data.aiJudge.reason='答案正确但缺步骤 2 验证 · 步骤 1,3 完整' · data.aiJudge.status='DONE' · data.aiJudge.final_grade_source='ai_accepted' (5 字段字面严匹配) |
| 6 | 用例 #5 fixture 同 review_plan + wb_review_node 行 (id=N6) 已 INSERT · **但 `ai_judge_verdict` 列显式为 NULL** (其他 4 satellite 列 user_answer_image_key/ai_judge_confidence/ai_judge_reason/ai_judge_metadata 可任意 · final_grade_source='self') · 模拟 fixture A: AI 判超时 (verdict 列 NULL 但 metadata.status='TIMEOUT') · 或 fixture B: 拍照上传成功但 AI 判异步未完成 · 或 fixture C: 学生纯自评从未走 AI 判路径 (但仍有 wb_review_node 行 e.g. SC20-T04 拍照上传未触发 judge) | HTTP client 调 `GET /api/review/nodes/N6/result` Header `X-User-Id:7` | 返 **HTTP 200** · ApiResult `data` 含 master §10.5 现役 13 字段不变 (向后兼容硬性) · **`aiJudge` 字段为 JSON null 字面** (不是 object 不是 undefined 不是 missing key · 严格 `"aiJudge": null`) · 因为 AC4 字面 "5 列任一为 null 时返 aiJudge=null" · 此用例 ai_judge_verdict 列 NULL · 命中此条件 · 拼装失败降级为 null · §10.19 字面 "向后兼容: AI 未判时为 null" · 满足 AC3 + AC4 + TI3 GET :result 旧客户端忽略 aiJudge 字段不崩 (JSON 兼容 · null 是合法 JSON 值 · 旧客户端 destructure `const { aiJudge = null } = data` 或不 destructure aiJudge key 均不抛 undefined warning) · DB SELECT 验证 `SELECT ai_judge_verdict FROM wb_review_node WHERE id=N6` 返 `NULL` 字面 (未被 GET 改 · GET 是 read-only) · 双重防御: aiJudge=null 是因为业务 AI 未判 · 不是因为代码 bug · 反作弊 grep response body 必含 `"aiJudge":null` 字面字符串 (JSON 序列化结果) · 不允许 Coder 漏返 aiJudge key (那是 missing key 不是 null · 旧客户端 `data.aiJudge` undefined 不是 null · 违反 §10.19 字面 "为 null") | n/a (无 frontend Console · 纯 backend · 取 Spring Boot log 0 [ERROR] + GET 200 OK + 不含 NullPointerException stack) | n/a (无 UI · 纯 backend) | GET /api/review/nodes/N6/result Header X-User-Id:7 → 200 · response body JSON 字面含 `"aiJudge":null` (严格字面字符串) · DB SELECT ai_judge_verdict FROM wb_review_node WHERE id=N6 → NULL (未被 GET 修改) |

## Changelog (TestDesigner 每轮 review 后追加)

<!-- 每轮 review 后追加 ## Round N · 改了什么 -->

## Round 1 · 初版

- TestDesigner agent (SC20-T03 attempt-1) 起草 · 6 用例 (满 7 列表头 audit · ≥3 ≤6 · 第 1 happy / 第 2-3 edge)
- 覆盖矩阵:
  - **AC1** (GradeReq DTO 加 final_grade_source `'self'|'ai_accepted'|'ai_overridden'` 缺省 'self'): 用例 #1 (ai_accepted 路径 · 字面传) + 用例 #2 (旧客户端不传 · default 'self' · 向后兼容硬性) + 用例 #3 (ai_accepted 字面违反 CHECK) + 用例 #4 (ai_overridden 路径)
  - **AC2** (GradeController POST :grade 落 final_grade_source 列到 wb_review_node · master SM-2 不破坏): 用例 #1 + #4 DB SELECT 后效字面验证 + 用例 #2 跨模块 IT 套件验证 master sibling 不破坏
  - **AC3** (NodeResultResp DTO 加 aiJudge 字段 含 verdict/confidence/reason/status/matched_steps?/missed_steps?/final_grade_source · null=AI 未判): 用例 #5 (完整 object 返) + 用例 #6 (null 返)
  - **AC4** (GET :result 从 wb_review_node 6 列拼装 aiJudge object · 5 列任一为 null 时返 aiJudge=null): 用例 #5 (5 列非空字面拼) + 用例 #6 (ai_judge_verdict 列 NULL 触发整体 null 降级)
  - **AC5** (应用层 CHECK · ai_accepted 时必 verdict===grade · ai_overridden 时必 verdict!=grade · self 时不校验): 用例 #3 (违反 → 422 GRADE_SOURCE_MISMATCH 非 500) + 用例 #4 (PASS · ai_overridden + verdict!=grade) + 用例 #1 (PASS · ai_accepted + verdict===grade)
  - **TI1** (向后兼容 · master sibling SC-01/T06/T11/HomeToday IT 全绿): 用例 #2 跨模块 IT 真跑 mvn verify exit 0
  - **TI2** (ai_accepted CHECK 违反返 422 非 500): 用例 #3 HTTP 422 字面 + DB transaction rollback 验证 (review_outcome 0 行 / outbox 0 行)
  - **TI3** (GET :result 旧客户端忽略 aiJudge 字段不崩 · JSON 兼容): 用例 #6 `"aiJudge":null` 字面字符串验证 + JSON destructure 不抛 undefined warning
  - **KI1** (改 grade 不破坏 master §7 SM-2 任何分支): 用例 #2 跨模块 IT (T06/T11/HomeToday 三个 master sibling IT) · 用例 #1 (PARTIAL 路径 ease/interval 走 master §10.5 现役) · 用例 #4 (FORGOT 路径 master §7 ease reset 2.5 · 不在用例字面强约束 ease 数值 · 因 master 现役实现是 SoT · 避免锁 ease 数值导致 SM-2 bug fix 误触发 REJECT)
  - **KI2** (final_grade_source 是 AI 判溯源唯一证据 · A.2 宪法): 用例 #4 (ai_overridden 落库 · RLHF outbox 推送是 SC-21 task 不在本 task) + 用例 #3 (CHECK 应用层防脏数据池 · A.2 宪法红线)
- 设计要点:
  - 用例 #1 happy 把 AC1+AC2+AC3+AC4 4 维度合并 (1 次 POST + 1 次 GET 串联) · 避免拆 4 个用例超 budget
  - 用例 #2 edge 把 AC1 向后兼容 + TI1 跨模块 IT + §10.18 字面 "default 'self' 旧客户端无感" 合并 · 跑现有 3 IT 套件 0 失败是 "fail loud" 保险栓
  - 用例 #3 是 AC5 CHECK 违反核心 · 字面要求 422 不 500 · DB transaction rollback 验证防 Coder 写半截 partial-write 留脏数据
  - 用例 #4 是 ai_overridden 合法路径 + KI1 master FORGOT 路径不破坏 + KI2 双信源溯源 · DB SELECT 字面 `ai_judge_verdict 仍 'MASTERED'` 验证 :grade 不动 ai_judge_* 5 列 (A.1 学生主体性铁律)
  - 用例 #5 是 GET happy 字面 5 字段 aiJudge object · 6 satellite 列拼装映射字面表达 (verdict ← ai_judge_verdict · status ← ai_judge_metadata.status JSONB 提取)
  - 用例 #6 是 GET edge null 降级 · `"aiJudge":null` 字面字符串验证 · 防 Coder 漏返 key (missing 不是 null · 旧客户端 undefined 不抛但语义不同 · §10.19 字面 "为 null" 严格)
- 故意可挑刺的点 (鼓励 Coder/Tester 找漏 · 不出"无可挑剔"的安全用例):
  - 用例 #1 Then 列 PARTIAL 路径 easeAfter 字面以 "master §10.5 现役实现为准" · 没锁数值 · reviewer 可能挑"应锁 -0.2 字面" · 但 master §7 SM-2 算法是 SoT · 锁数值是 SC-08/SC-09 task 范围 · 本 task 不破坏 SM-2 是 KI1 真正语义
  - 用例 #2 wb_review_node 行**不前置 INSERT** · 但 :grade 改造后是否要 UPSERT 创建 wb_review_node 行 · 还是只在有 AI 判时才创建 · Coder Step 4 实现选择 · TestDesigner 给"二选一不能两态共存"的硬约束 · reviewer 可能挑"这二态都不写死会导致 Coder 选择面太大"
  - 用例 #3 ApiResult 失败 code 数字字面未锁 (e.g. 42201 vs 4xxxx mismatch 关键字) · reviewer 可能挑应锁字面 code · 但项目错误码字典是 master §11 现役范围 · 本 task 不应擅自定义新 code · 锁 HTTP 422 status + msg 关键字 'GRADE_SOURCE_MISMATCH' 已够强
  - 用例 #5 ai_judge_metadata JSONB 字段 status 提取 (aiJudge.status ← ai_judge_metadata.status) · 字面映射规则未在 §10.19 显式写 · 但 §4.16 字面 ai_judge_metadata JSONB 含 status 字段 · 推论 aiJudge.status 由此拼 · reviewer 可能挑"应在 §10.19 显式定义 aiJudge.status 字段语义" · 但 spec 是 SoT · 不擅自补 spec · 推论可接受
  - 用例 #6 fixture 3 选 1 路径 (AI 超时 / 异步未完成 / 无 AI 判) 都触发 ai_judge_verdict=NULL → aiJudge=null 降级 · 但 fixture C "学生纯自评但仍有 wb_review_node 行" 这个组合现实存在性低 (因 master §10.5 现役不写 wb_review_node 表 · 直到 SC20-T04 拍照路径) · reviewer 可能挑 fixture C 不实
  - matched_steps / missed_steps 可选字段未锁字面 · TestDesigner 给"空数组或不返均符 §10.19 ? 标记" · reviewer 可能挑应锁二选一
- 故意不做 (越界 / 留给后续 task):
  - 不验 POST /api/review/nodes/{nid}/judge 新接口 (那是 SC-20-T0X AnswerJudgeService task)
  - 不验 RocketMQ ai-judge.overridden outbox 推送 (那是 SC-21-T0X RLHF outbox task)
  - 不验前端 P08 `<AiJudgeBanner>` 渲染 (那是 SC-20-T0Y frontend mp task)
  - 不验 OSS upload 30 天 lifecycle rule (那是 OPS task)
  - 不验 final_grade_source 应用层枚举校验 (e.g. 传 'invalid_source' 字面) · TestDesigner 视为 Coder Step 4 默认会做 enum check (DTO 用 record + @JsonValue + enum class 或 String + 枚举字面校验) · 不在用例字面强约束
  - 不验 master §7 SM-2 ease/interval 字面数值 (那是 SC-01-T11 / SC-08 / SC-09 task)
  - 不验 idempotency-key 重放 (master §10.5 现役已有 · 本 task 不破坏)
